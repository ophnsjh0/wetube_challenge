doctype html
html(lang="ko")
    head
        title #{pageTitle} 
        link(rel="stylesheet", href="https://unpkg.com/mvp.css")
    body
        header
            h1=pageTitle
            form(method="POST", enctype="multipart/form-data", action="/read" )
              label(for="text") Text
              input( type="file", id="text", name="text", accept="text/plain", required)
              input(type="submit", value="Conver")